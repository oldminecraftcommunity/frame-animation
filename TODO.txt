a?.?.?
- Add server animations (as a suggestion).
- Add loop mode to client.
- Proxy to use animations on unsupported servers.
- Translate and scale functions (and a setting to disable/limit them in server settings or smh idk).
- Reload button in edit mode.
- Rework mods base code: 
1. move compat checks to some other api.
2. add compiled animations to server.
3. rework networking to avoid sending a lot of data packets as they can be f*cking huge!
4. internal error checker with notification system to avoid silent errors/showing errors in console.
5. general code cleanup.